import ServicePage from "./ServicePage";

export default function Skin() {
  return (
    <ServicePage
      pageKey="skin"
      accent="#1f6f6b"
      fallbackTitle="Skin Care & Dermatology"
      fallbackDescription="Dermatology care built around your skin, not a generic checklist."
    />
  );
}
